//! Navigation database builder — stub.
//!
//! Full implementation in Phase 4 (plan 04). This stub satisfies the Phase 1
//! requirement that the crate compiles and `cargo test` passes.

fn main() {
    eprintln!("nav-data-builder: not yet implemented (Phase 4)");
    std::process::exit(1);
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Ensures `cargo test` does not fail with "no tests" warning.
        assert_eq!(2 + 2, 4);
    }
}
