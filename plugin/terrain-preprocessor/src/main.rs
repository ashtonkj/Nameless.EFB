//! Terrain tile preprocessor — stub.
//!
//! Full implementation in Phase 4 (plan 04). This stub satisfies the Phase 1
//! requirement that the crate compiles and `cargo test` passes.

fn main() {
    eprintln!("terrain-preprocessor: not yet implemented (Phase 4)");
    std::process::exit(1);
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        assert_eq!(2 + 2, 4);
    }
}
