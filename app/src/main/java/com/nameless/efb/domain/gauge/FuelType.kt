package com.nameless.efb.domain.gauge

/** Fuel type determines density for flow and quantity conversions. */
enum class FuelType {
    /** AVGAS 100LL — density 0.72 kg/L */
    AVGAS,
    /** Jet A-1 — density 0.80 kg/L */
    JET_A1,
}
